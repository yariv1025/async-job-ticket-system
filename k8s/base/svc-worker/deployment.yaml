apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-worker
  namespace: jobsys
  labels:
    app: svc-worker
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-worker
  template:
    metadata:
      labels:
        app: svc-worker
        component: worker
    spec:
      containers:
      - name: svc-worker
        # image: #TODO:248189908494.dkr.ecr.us-east-1.amazonaws.com/svc-worker:latest
        image: docker.io/library/svc-worker:latest # local image
        imagePullPolicy: Never #Always
        env:
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: AWS_REGION
        - name: ENV
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: ENV
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: LOG_LEVEL
        - name: DDB_TABLE
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: DDB_TABLE
        - name: SQS_QUEUE_URL
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: SQS_QUEUE_URL
        - name: MAX_MESSAGES
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: MAX_MESSAGES
        - name: WAIT_TIME_SECONDS
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: WAIT_TIME_SECONDS
        - name: AWS_ENDPOINT_URL
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: AWS_ENDPOINT_URL
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_DEFAULT_REGION
          valueFrom:
            configMapKeyRef:
              name: svc-worker-config
              key: AWS_DEFAULT_REGION
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi

